<nav class="navbar navbar-default" role="navigation">
	<div class="container-fluid">
		<div class="navbar-header">
			<%= link_to 'JamBase App', '#', :class => 'navbar-brand' %>
		</div>

		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
<%
=begin%>
				<% @recipients.each do |recipient| %>
				<li class="dropdown">
					<%= link_to (recipient["displayName"] + '<b class="caret"></b>').html_safe, '#', :'data-toggle' => :'dropdown', :class => 'dropdown-toggle' %>
					<ul class="dropdown-menu">
						<% recipient["childRecipients"].each do |child| %>
						<li><%= link_to child["displayName"], '#' %></li>
						<% end %>
					</ul>
				</li>
				<% end %>
<%
=end%>
			</ul>

			<div class="nav navbar-nav navbar-right">
				<ul class="nav navbar-nav navbar-right">
					<% if signed_in? %>
						<li><%= link_to 'Sign Out', signout_path %></li>
					<% else %>
						<li><%= link_to 'Sign In', signin_path %></li>
					<% end %>
				</ul>
			</div>
		</div>
	</div>
</nav>